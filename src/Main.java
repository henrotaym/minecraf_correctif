import java.util.Optional;
import java.util.Random;

import Helpers.RandomHelper;
import entities.Inventory;
import entities.InventorySlot;
import entities.Item;
import enums.ItemType;
import exceptions.enums.ItemTypeException;

public class Main {
    public static void main(String[] args) throws ItemTypeException {
        // Item item = new Item("arc", ItemType.WEAPON);
        // InventorySlot slot = new InventorySlot(Optional.of(item));
        // InventorySlot[] slots = { slot, slot, slot, slot, slot };
        // Inventory inventory = new Inventory(slots);
        // inventory.print();
    }
}